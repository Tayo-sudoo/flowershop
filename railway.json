{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn flowershop.wsgi --workers 2 --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}